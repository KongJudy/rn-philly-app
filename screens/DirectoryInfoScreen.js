import RenderAttraction from '../features/attractions/RenderAttraction';

const DirectoryInfoScreen = (props) => {
  return <RenderAttraction attraction={props.attraction} />;
};

export default DirectoryInfoScreen;
